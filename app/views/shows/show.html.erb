<h1><%= @show.title %></h1>
<p><%=@show.body%></p>

<%@beat.each do |beat| %>

  <ul>

  <%=link_to "#{beat.title}", team_show_beat_path(@team, @show,beat)%>

  <%beat.notes.each do |note| %>
    <li><%=note.body%></li>
  <%end%>
</ul>
<%end%>


<p><%= link_to "Add Beat", new_team_show_beat_path(@team, @show) %></p>

<p><%= link_to "Edit show", edit_team_show_path(@team, @show) %></p>
<div class = "navLink"><%= link_to "Back to A ll Shows", team_shows_path %></div>
