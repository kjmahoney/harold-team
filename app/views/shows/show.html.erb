<h1><%= @show.title %></h1>
<p><%=@show.body%></p>

<%@beat.each do |beat| %>

  <ul>

  <%=link_to "#{beat.title}", show_beat_path(@show,beat)%>

  <%beat.notes.each do |note| %>
    <li><%=note.body%></li>
  <%end%>
</ul>
<%end%>




<p><%= link_to "Add Beat", new_show_beat_path(@show) %></p>

<p><%= link_to "Edit show", edit_show_path(@show) %></p>
<div class = "navLink"><%= link_to "Back to A ll Shows", shows_path %></div>
