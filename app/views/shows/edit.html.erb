<h1><%= @show.title %></h1>
<%= render "form"%>


<%= form_for [@show], method: :delete, data: {confirm: "Are you sure?"} do |s|%>
  <%= s.submit "Delete show" %>
<% end %>

<div class = "navLink"><%= link_to "Back", show_path(@show) %></div>
