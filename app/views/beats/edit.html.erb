<h1>Beats</h1>
<%= render "form" %>

<div class = "navLink"><%= link_to "Back to Show", team_show_path(@team, @show) %></div>

<%= form_for [@team, @show,@beat], method: :delete, data: {confirm: "Are you sure?"} do |s|%>
  <%= s.submit "Delete beat" %>
<% end %>
